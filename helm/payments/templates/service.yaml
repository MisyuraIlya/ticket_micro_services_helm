apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}-srv
spec:
  selector:
    app: {{ .Chart.Name }}
  ports:
    - name: payments
      protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
